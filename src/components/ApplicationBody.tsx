import React from "react";
import { AppInfoProps } from "../types/application";

const ApplicationBody: React.FC<AppInfoProps> = () => {
  return (
    <section>
      Body
    </section>
  )
}

export default ApplicationBody;